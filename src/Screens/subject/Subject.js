import React from "react";
import { SafeAreaView, ScrollView } from 'react-native';
const Subject = () => {
    return (
        <SafeAreaView>
            <ScrollView>

            </ScrollView>
        </SafeAreaView>
    )
}
export default Subject;